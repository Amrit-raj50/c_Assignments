#include <stdio.h>
int main(){
    int n = 4;
    for(int i = 4 ; i >= 7 ; i--)
    {
        for(int j = 1 ; j >= i ; j--)
        {
            if(j < )
            printf("  ");
            else
            printf("%d ",j); 
        }
        printf("\n");
    }
    return 0;
}